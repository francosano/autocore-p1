{
  "name": "autocore-p1-extension",
  "version": "0.1.0",
  "private": true,
  "description": "AutoCore P1 — Facebook Marketplace bridge (Chrome MV3, human-in-the-loop)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "build:ext": "node build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
